export enum COOKIES {
  ACCESS_TOKEN = 'access_token',
  REFRESH_TOKEN = 'refresh_token',
  SIDEBAR_STATE = 'sidebar_state',
  LOCALE = 'locale',
  ROLE = 'role',
}

export enum ROLE {
  ADMIN = 'ADMIN',
  MEMBER = 'MEMBER',
}
